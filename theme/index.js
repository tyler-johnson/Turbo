// Load in Theme; if other objects need to be used load "./theme/OBJECT"
var Theme = require('./theme');
	
// Load in the utility file for extra goodies
require('./utility');

// Go!
module.exports = Theme;